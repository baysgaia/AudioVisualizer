# python/requirements.txt

# -----------------------------------------------------
# 基本的にはPyTorch (torch, torchvision, torchaudio) は
# Apple Silicon (M1/M2系) の場合、公式インストールガイド
# を参照して別途インストールする方が望ましいです。
# -----------------------------------------------------

# DEMUCS (音源分離などに使用)
demucs>=4.0.0

# Whisper (実際には PyPI 上では openai-whisper という名前)
openai-whisper>=2023.2.2

# Librosa (オーディオ信号処理ライブラリ)
librosa>=0.10.0

# MIDI操作ライブラリ
mido>=1.2.10

# 必要に応じて、F0推定などに使う CREPE
crepe>=0.0.15
# 必要に応じて、ビデオ生成に使用する FFmpeg
ffmpeg
